<!DOCTYPE html>
<html lang="ru" style="height: 100%;">
<head>
    <meta charset="utf-8">
    <title>Обработка...</title>
    <meta
            name="viewport"
            content="width=device-width, initial-scale=1, minimum-scale=1, maximum-scale=1, viewport-fit=cover"
    />
    <style>
        * {
            box-sizing: border-box;
            margin: 0;
        }
    </style>
</head>
<body style="display: flex; width: 100%; height: 100%; align-items: center; justify-content: center; font-family: sans-serif;">
<% if (typeof error !== 'undefined' && error) { %>
    <div style="max-width: 24em; text-align: center;">
        <p>Произошла ошибка входа</p>
        <% if (typeof errorInfo !== 'undefined' ) { %>
            <p><%= errorInfo %></p>
        <% } %>
        <% if (typeof errorId !== 'undefined' ) { %>
            <p>Код ошибки: <%= errorId %></p>
        <% } %>
        <p>Попробуйте ещё раз</p>
        <p>Если ошибка повторяется, обратитесь в поддержку</p>
    </div>
<% } else { %>
    <%- include('./spinner.ejs') %>
    <script>
      if (window.opener && window.opener.postMessage) {
        window.opener.postMessage(
                <%- message %>,
                <%- origin %>
        );
      }
    </script>
<% } %>
</body>
</html>
